<Page x:Class="In_Extremis.Editor.ComplexPlanePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:In_Extremis.Editor"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="ComplexPlanePage">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="5*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="12*"/>
            <RowDefinition />
        </Grid.RowDefinitions> Grid.Column="1" Grid.Row="0"  -->
        <local:ComplexPlane  x:Name="ComplexPlane" Radius="50" Loaded="ComplexPlane_Loaded"/>
        <!--<ListBox Name="Numbers" Grid.Column="0" Grid.Row="0" Margin="3">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding LatticePointsOnSquareRootOfN}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox> -->
    </Grid>
</Page>
